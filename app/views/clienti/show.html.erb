<div class="row">
	<div class="col-lg-6">
		<div class="well well-lg">

				
				<!-- -->
					<div class="dropdown pull-right">
					  <button class="btn btn-primary dropdown-toggle" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
					    Azioni
					    <span class="caret"></span>
					  </button>
					  <ul class="dropdown-menu" aria-labelledby="dropdownMenu1">
					    <li><%= link_to 'Modifica', edit_clienti_path(@clienti) %></li>
					    <li><%= link_to 'Elimina', clienti_path(@clienti), method: :delete, data: { confirm: 'Sicuro di voler eliminare il cliente? Non è reversibile' } %></li>
					    <!--<li><%= link_to 'Invia Comunicazione', root_path %></li> -->
					  </ul>
					</div>

			 	<!-- 	-->
			 	<h4>Cliente: <%= @clienti.nome %></h4>

			 	<ul class="list-unstyled">
					<li><span>P.iva: </span><%= @clienti.piva%></li>
					<li><span>C.F.: </span><%= @clienti.codfisc%></li>
					<li><span>E-Mail: </span><%= @clienti.email%></li>
					<li><span>Telefono: </span><%= @clienti.telefono%></li>
					<li><span>Login: </span>
						<%= @clienti.utenti ? @clienti.utenti.id : "---ATTENZIONE--- Nessuna login associata !!".html_safe %>
					</li>
				</ul>

		</div>
	</div>
	<div class="col-lg-6">
		<div class="well well-lg">
			<div class="row">
				<div class="col-md-6">
					<%= button_tag '<i class="fa fa-comments-o"></i>Interventi <span class="badge badge-danger"> 33 </span>'.html_safe, class: 'btn btn-icon btn-block' %>
				</div>
				<div class="col-md-6">
					<%= link_to '<i class="fa fa-comments-o"></i>Contratti <span class="badge badge-success"> 44 </span>'.html_safe, clienti_contrattis_path(:clienti_id => @clienti.id), class: 'btn btn-icon btn-block' %>

					<%= button_tag 'prova',  {:type => 'button', :controller => "interventi", :action => "index", :clienti_id => @clienti.id } %>
				</div>
			</div>
			<div class="row"></div>

		</div>
	</div>
</div>

<div class="divider"></div>
<!--
<%# per linkare alla pagina successiva, modificando un parametro di post, traducendo quindi il pramaetro "clienti.id" fornito in quest pagina in "clienti_id" come richiesto dalla pagina successiva %>
<%#= link_to 'Aggiungi Intervento', new_clienti_interventi_path(:clienti_id => @clienti.id) %>
-->
<div class="row">
	<div class="col-lg-12">
		<%= link_to 'Indietro', clienti_index_path, class: 'btn btn-danger' %>
		<%= link_to 'Gestione Contratti', clienti_contrattis_path(:clienti_id => @clienti.id), class: 'btn btn-purple' %>
		<%= link_to 'Elenco Interventi', clienti_interventi_index_path(:clienti_id => @clienti.id), class: 'btn btn-primary' %>
		<%= link_to 'Nuovo Intervento', new_clienti_interventi_path(:clienti_id => @clienti.id), class: 'btn btn-success' %>
		<%= link_to '<i class="fa clip-user-3"></i> Associa Login'.html_safe, new_utenti_path( :varcliente => @clienti.id), class: 'btn btn-bricky' if !@clienti.utenti %>
	</div>
</div>
<div class="divider"></div>

<%= debug(params) %>

<script>
			jQuery(document).ready(function() {
				Main.init();
				//TableData.init();
				//UIElements.init();
				//FormElements.init();
				UIButtons.init();
			});
	</script>