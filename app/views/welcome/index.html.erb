<h1>Welcome#index</h1>
<p>Find me in app/views/welcome/idex.html.erb</p>

<br>
<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>
<br>


<p>Ci sono <%= @conta_aperti %> interventi aperti. Clicca <%= link_to 'quì', welcome_aperti_path %> per visualizzarli</p>
<hr>
<p>Hai un credito residuo per gli sms di €<%= @credito_skebby.round(2) %></p>
<hr>
<p><%= link_to "Nuova pagina di login", welcome_login_path %></p>

<% if utenti_signed_in? %>
	<p>Sei loggato come:<%= current_utenti.email  %> </p>
<% end %>

<% flash.each do |key, value| %>
		<hr>
        <div class="alert alert-<%= key %>">
        	<%= key %> : <%= value %>
        </div>
		<hr>
	<% end %>

<hr>

<p>Sono Guest = <% if !utenti_signed_in? %> <%= 'si' %> <% else  %> <%= 'no' %> 	<% end	%> | </p>

<p>Sono loggato =  <% if utenti_signed_in? %> <%= 'si' %> <% else  %> <%= 'no' %> 	<% end	%> | </p>

<p>Amministratore = <% if utenti_signed_in? && current_utenti.admin? %> <%= 'si' %> <% else  %> <%= 'no' %> 	<% end	%> | </p>

<p>Sono Operatore = <% if utenti_signed_in? && (current_utenti.admin? || current_utenti.operatore? ) %> <%= 'si' %> <% else  %> <%= 'no' %> 	<% end	%> | </p>


aa: <%= @sono_operatore %>

<!-- 	-->

<h4>Da fare</h4>

<p>Permessi per operatore<br>
da operatore: elenco interventi assegnati con evidenza per nuovi e aperti<br></p>

<p>Togliere "interventi aperti" da controller welcome</p>



<!-- 	-->
<hr>




<%= debug(params) %>

<script>
	function cookie_barra_aperta() {
		document.cookie = 'navigation=aperta'; 
	}

	function cookie_barra_chiusa() {
		document.cookie = 'navigation=chiusa'; 
	}

</script>

